[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && npm install && npm run build && cp -r dist ../backend/public && cd ../backend && npm install"

[deploy]
startCommand = "node backend/src/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
