{
  "name": "kabarman",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev --legacy-peer-deps && npm run build && cp -r dist ../backend/public && cd ../backend && npm install",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
